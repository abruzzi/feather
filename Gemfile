source :rubygems

gem 'sinatra'
gem 'haml'

gem 'omniauth'
gem 'omniauth-github'

gem 'rack-contrib'

gem 'datamapper'

group :production do
    gem 'pg'
    gem 'dm-postgres-adapter'
end

group :test do
    gem 'sqlite3'
    gem 'dm-sqlite-adapter'
    gem 'rspec'
    gem 'rack-test'
    gem 'factory_girl'
end
