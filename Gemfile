source :rubygems

gem 'sinatra'
gem 'haml'

gem 'omniauth'
gem 'omniauth-github'

gem 'datamapper'

group :production do
    gem 'pg'
    gem 'dm-postgres-adapter'
end

group :development, :test do
    gem 'shotgun'
    gem 'dm-sqlite-adapter'
end
