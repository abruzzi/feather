source :rubygems

gem 'sinatra'
gem 'omniauth'
gem 'omniauth-github'
gem 'omniauth-openid'

gem 'datamapper'

gem 'builder'

gem 'haml'

group :production do
    gem 'pg'
    gem 'dm-postgres-adapter'
end

group :development, :test do
    gem 'shotgun'
    gem 'dm-sqlite-adapter'
end
