.content
  %h2 Add notes here for remind
  %section#add
    %form{:action => '/notes', :method => 'POST'}
      %textarea{:name => 'content', :placeholder => 'note here...'}
      %input.btn.btn-primary{:type => 'submit', :value => 'note'}

  .notes-container
    - @notes.each do |note|
      .well{:class => note.complete ? 'alert-success' : 'alert-info'}
        = note.content
        %p{:class => 'muted'}
          created: "#{note.created_at}"
        .pull-right
          %a{:href => "/notes/#{note.id}"} [Edit]
          %a{:href => "/notes/#{note.id}/delete"} [Delete]
          %a{:href => "/notes/#{note.id}/complete"} [Complete]
